# MewCode 开发前置约定

MewCode 采用 Spec 驱动开发。每个阶段先完成并审校四份文档，再按任务清单实现和验收：

```text
spec.md（做什么）→ plan.md（怎么做）→ task.md（按什么顺序做）→ checklist.md（做对了没）
```

## 仓库约定

- `MewCode/` 是唯一项目根；所有阶段共享根目录的 `pyproject.toml`、`.venv/`、
  `src/mewcode_cli/`、`tests/` 和 `.mewcode/`。
- 阶段目录只存文档，命名为 `docs/phases/phase-NN-topic/`，例如第一阶段为
  `docs/phases/phase-01-chat/`。
- 后续阶段在现有实现上增量开发，不复制或嵌套新的 Python 项目。
- 文档里的实现路径都以仓库根目录为基准。
- Python 包名、CLI 命令和应用名统一为 `mewcode` / `MewCode`。
- 真实密钥只允许存在于被 Git 忽略的 `.mewcode/config.yaml`；仓库只提交示例配置。

## 四份文档的职责

| 文档 | 回答的问题 | 必须包含 |
|---|---|---|
| `spec.md` | 做什么 | 背景、目标、功能和非功能需求、边界、验收标准 |
| `plan.md` | 怎么做 | 架构、接口、数据流、文件组织、技术决策 |
| `task.md` | 按什么顺序做 | 文件清单、有序任务、依赖和每项验证方式 |
| `checklist.md` | 做对了没有 | 自动检查、集成检查、真实端点和端到端场景 |

## 开发门槛

1. 四份文档必须完整，不保留 TBD/TODO、外层代码围栏或复制项目的路径和术语。
2. `spec.md` 的每条功能需求必须至少对应一条可观测验收标准。
3. `plan.md` 的每个组件必须在 `task.md` 中有实现和验证任务。
4. 开发按 `task.md` 执行；验证通过后才能标记完成。
5. 最终按 `checklist.md` 逐项记录实际结果。需要真实凭据但当前无法执行的项目应明确
   标记“待凭据验证”，不能按预期结果伪报通过。

## 文档自检

- 路径是否以当前 MewCode 根目录为准，而不是复制来源的目录？
- 是否混入其它语言或框架术语，例如 Go、Bubble Tea、lipgloss、tmux 强依赖？
- 接口、文件名和数据结构在 `plan.md` / `task.md` 中是否一致？
- 是否把框架能力写成了不存在的承诺，例如全屏 TUI 退出后保留原生 scrollback？
- 是否区分了自动测试和必须连接真实服务的手工验收？
- 是否先有命令输出或可观察行为，再报告“已通过”？

## 开发与验收

根目录共享虚拟环境：

```powershell
python -m venv .venv
.\.venv\Scripts\python -m pip install -e ".[dev]"
.\.venv\Scripts\python -m ruff format --check .
.\.venv\Scripts\python -m ruff check .
.\.venv\Scripts\python -m pytest -q
```

运行期配置从根目录 `.mewcode/config.yaml` 读取。任何测试、日志、截图和验收记录都不得
包含真实 API key。
